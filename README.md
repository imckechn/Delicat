# Delicat

- Run frontend locally:
    cd frontend && npm start

- Run api locally:
    cd api && flask run

- Run whole project locally:
    cd api && flask run
    // open new terminal window, then run:
    cd frontend && npm start